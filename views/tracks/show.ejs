<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title><%= track.title %> by <%= track.artist.name %></title>
    <link rel="stylesheet" href="/css/main.css">
</head>
<body>
    <header>
        <h1><%= track.title %></h1>
    </header>
        <main>
            <section>
                <h1>Artist: <a href="/artists/<% track.artist._id %>"><%= track.artist.name %></a></h1>

                <a href="<%= track.link %>">Play <%= track.title %></a><br>
                <a href="<%= track.link %>"><img src="<%= track.image %>"></a>
            </section>
            <section>
                <a href="/tracks/<%= track._id %>/edit">Edit track</a>
            </section>
            <nav>
                <ul>
                    <li>
                        <a href="/">Home</a>
                    </li>
                    <li> 
                        <a href="/tracks">All Tracks</a>
                    </li>
                </ul>
            </nav>
            <section>
                <form action="/tracks/<%= track._id %>?_method=DELETE" method="POST">
                <input type="submit" value="Delete Track"/>
                </form>
            </section>
        </main>
</body>
</html>